{
  "name": "red-tetris",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "prod": "npm --workspace=client run build && npm --workspace=server run build && npm --workspace=server run prod",
    "coverage": "npm --workspace=server run coverage"
  }
}
